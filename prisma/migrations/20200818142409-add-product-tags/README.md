# Migration `20200818142409-add-product-tags`

This migration has been generated by Marco Wettstein at 8/18/2020, 4:24:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "ProductTag" (
"id" TEXT NOT NULL,
"title" TEXT NOT NULL,
PRIMARY KEY ("id"))

CREATE TABLE "OrderItem" (
"id" TEXT NOT NULL,
"productId" TEXT NOT NULL,
"quantity" INTEGER NOT NULL DEFAULT 1,
"orderId" TEXT NOT NULL,
PRIMARY KEY ("id"),
FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("orderId") REFERENCES "Order"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Order" (
"id" TEXT NOT NULL,
"created" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" TEXT ,
PRIMARY KEY ("id"),
FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "_ProductToProductTag" (
"A" TEXT NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "ProductTag"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "Order.userId_unique" ON "Order"("userId")

CREATE UNIQUE INDEX "_ProductToProductTag_AB_unique" ON "_ProductToProductTag"("A","B")

CREATE  INDEX "_ProductToProductTag_B_index" ON "_ProductToProductTag"("B")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200818124828-order..20200818142409-add-product-tags
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator prisma_client {
   provider        = "prisma-client-js"
@@ -31,15 +31,22 @@
   cart      Cart?
   Order     Order[]
 }
+model ProductTag {
+  id       String    @default(uuid()) @id
+  title    String
+  products Product[]
+}
+
 model Product {
-  id        String      @default(uuid()) @id
-  slug      String      @unique
+  id        String       @default(uuid()) @id
+  slug      String       @unique
   title     String
   imageUrl  String
   cartItems CartItem[]
   OrderItem OrderItem[]
+  tags      ProductTag[]
 }
 model CartItem {
   id        String  @default(uuid()) @id
```


