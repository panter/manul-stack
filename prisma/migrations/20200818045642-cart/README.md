# Migration `20200818045642-cart`

This migration has been generated by Claudio Romano at 8/18/2020, 6:56:42 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "CartItem" (
"id" TEXT NOT NULL,
"productId" TEXT NOT NULL,
"quantity" INTEGER NOT NULL,
"cartId" TEXT NOT NULL,
PRIMARY KEY ("id"),
FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("cartId") REFERENCES "Cart"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Cart" (
"id" TEXT NOT NULL,
"created" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" TEXT ,
PRIMARY KEY ("id"),
FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "Cart.userId_unique" ON "Cart"("userId")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200818041245-product-slug-unique..20200818045642-cart
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -26,17 +26,36 @@
   email     String     @unique
   password  String
   blogPosts BlogPost[]
   roles     UserRole[]
+  cart      Cart?
 }
 model Product {
-  id       String @default(uuid()) @id
-  slug     String @unique
-  title    String
-  imageUrl String
+  id        String     @default(uuid()) @id
+  slug      String     @unique
+  title     String
+  imageUrl  String
+  cartItems CartItem[]
 }
+model CartItem {
+  id        String  @default(uuid()) @id
+  product   Product @relation(fields: [productId], references: [id])
+  productId String
+  cart      Cart    @relation(fields: [cartId], references: [id])
+  quantity  Int
+  cartId    String
+}
+
+model Cart {
+  id        String     @default(uuid()) @id
+  created   DateTime   @default(now())
+  user      User?      @relation(fields: [userId], references: [id])
+  userId    String?    @unique
+  cartItems CartItem[]
+}
+
 model Page {
   id                 String   @default(uuid()) @id
   published          Boolean? @default(false)
   createdAt          DateTime @default(now())
```


