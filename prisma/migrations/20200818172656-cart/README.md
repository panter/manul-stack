# Migration `20200818172656-cart`

This migration has been generated by Claudio Romano at 8/18/2020, 7:26:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "CartItem" (
"id" TEXT NOT NULL,
"productId" TEXT NOT NULL,
"cartId" TEXT ,
PRIMARY KEY ("id"),
FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("cartId") REFERENCES "Cart"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Cart" (
"id" TEXT NOT NULL,
"created" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200818164151-product-slug-unique..20200818172656-cart
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -59,9 +59,24 @@
   @@unique([parentPageId, slug])
 }
 model Product {
-  id          String @default(uuid()) @id
-  slug        String @unique
+  id          String     @default(uuid()) @id
+  slug        String     @unique
   title       String
   description String
+  cartItems   CartItem[]
 }
+
+model CartItem {
+  id        String  @default(uuid()) @id
+  product   Product @relation(fields: [productId], references: [id])
+  productId String
+  cart      Cart?   @relation(fields: [cartId], references: [id])
+  cartId    String?
+}
+
+model Cart {
+  id        String     @default(uuid()) @id
+  created   DateTime   @default(now())
+  cartItems CartItem[]
+}
```


