name: CI
on: [push]
jobs:
  build:
    name: Test & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v2-beta
        with:
          node-version: "12"
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2-beta
        with:
          cmd: install
      - uses: actions/setup-node@v2-beta
        with:
          cmd: lint
      - uses: actions/setup-node@v2-beta
        with:
          cmd: test
